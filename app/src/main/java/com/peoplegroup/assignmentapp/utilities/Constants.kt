package com.peoplegroup.assignmentapp.utilities

object Constants {
    /**
     *  To decide how many records will be fetched in one iteration
     *  if no other value is passed from view model.
     */
    const val PERSON_DEFAULT_LIMIT = 10
}